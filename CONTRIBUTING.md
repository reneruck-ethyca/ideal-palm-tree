# Contributing

## Running tests

Tests use Node's built-in test runner and live alongside app code as
`*.test.mts` files under `app/`.

Run the full suite with:

```sh
npm test
```

This runs `node --test "app/**/*.test.mts"`.
